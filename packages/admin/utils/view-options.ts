export enum ViewOptions {
  table = "table",
  grid = "grid",
  mobile = "mobile",
}

export const DEFAULT_VIEW_OPTIONS = [ViewOptions.table];

export const ALL_VIEW_OPTIONS = [ViewOptions.table, ViewOptions.grid];
