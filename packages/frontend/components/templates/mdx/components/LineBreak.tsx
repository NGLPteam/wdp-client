/**
 * This tag operates as a simple wrapper around `<br/>` tags,
 * which are difficult to produce properly from the API during
 * its sanitization process.
 */
export default function LineBreak() {
  return <br />;
}
