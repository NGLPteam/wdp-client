export const aHidden = `
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0 0 0 0);
`;

export const aInputReset = `
  appearance: none;
  border: none;
  cursor: pointer;
  background: transparent;
  color: inherit;
`;
