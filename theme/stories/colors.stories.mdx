import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";
import { colors } from "../base/variables";

<Meta title="Styles/Colors" />

# Colors

<ColorPalette>
  {Object.entries(colors).map(([colorKey, colorList], i) => {
    return <ColorItem key={i} title={colorKey} colors={colorList} />;
  })}
</ColorPalette>
